#include<stdio.h>
#include<string.h>

#define LEN 128

int htoi(char *s);

int main()
{
		char s[LEN];
		char *ps;
		ps = s;
		int number;
		printf("请输入一个十六进制字符串:\n");
		scanf("%s",ps);
		fputs(ps,stdout);

		number = htoi(ps);


		return 0;
}

int htoi(char *s)
{
		
		return -1;
}
